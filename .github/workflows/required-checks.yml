name: required-checks

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
      - ready_for_review

jobs:
  validate:
    if: github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run required checks
        run: pnpm check:full

      - name: Write live upload .env
        run: |
          cat <<EOF > .env
          FAREWELL_UPLOAD_E2E=1
          FAREWELL_UPLOAD_E2E_GITHUB_TOKEN=${{ secrets.FAREWELL_UPLOAD_E2E_GITHUB_TOKEN }}
          EOF

      - name: Run live upload Playwright e2e
        run: pnpm test:network:upload

      - name: Run coverage gate
        run: pnpm test:coverage

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          use_oidc: true
          files: ./coverage/lcov.info
          fail_ci_if_error: true
