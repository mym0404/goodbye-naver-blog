# Design QA

## 비교 대상

- 원본 화면: `tmp/design-qa/source-before.png`
- 구현 화면: `tmp/design-qa/implementation-946x824-dark.png`
- URL: `http://localhost:4317/`
- 뷰포트: `946×824`
- 상태: 다크 테마, Naver Blog 선택, 입력 전 기본 상태

## 비교 근거

- 전체 화면 비교: `tmp/design-qa/full-comparison.png`
- 폼 영역 비교: `tmp/design-qa/form-comparison.png`
- 모바일 확인: `tmp/design-qa/implementation-375x812-light.png`
- 넓은 화면 확인: `tmp/design-qa/implementation-1440x900-light.png`

전체 화면에서는 헤더와 본문의 정보 구조, Primer 표면, 상태 색상, 버튼 위치를 비교했다. 폼 영역에서는 선택값 정렬, 필드 순서, 레이블과 입력 사이 간격, 세로 리듬을 확대해 확인했다.

## 확인 결과

- P0, P1, P2 문제는 없다.
- 폰트와 글자 계층은 기존 Primer·Geist 설정을 그대로 사용해 원본과 같다.
- 폼은 모든 화면 너비에서 한 열로 이어지고, 레이블·입력·도움말 순서를 유지한다.
- 선택 메뉴의 값은 왼쪽에 정렬되고 펼침 표시는 오른쪽에 남는다.
- 앱 헤더와 본문은 같은 `768px` 최대 폭을 사용하며 넓은 화면에서 가운데 정렬된다.
- 기존 Primer 색상 토큰, 테두리, 반경, 상태 색상을 유지해 라이트·다크 테마의 대비가 달라지지 않았다.
- 원본과 구현 화면에는 검증할 사진이나 래스터 이미지가 없다. Octicon과 Primer 아이콘은 기존 자산을 그대로 사용했다.
- 앱 문구와 입력값은 바꾸지 않았다.
- 모바일 화면에서 가로 넘침이 없고, 입력과 버튼이 한 열로 읽힌다.
- Naver Blog에서 Tistory로 선택을 바꾸면 선택값과 URL 예시가 함께 바뀐다.
- 브라우저 콘솔의 오류와 경고는 없다.

## 비교 이력

- 첫 비교에서 수정이 필요한 P0, P1, P2 문제를 찾지 못했다.
- 구현 후 시각 수정은 하지 않았으며, 최종 캡처는 원본과 같은 뷰포트와 상태로 다시 저장했다.

## 남은 범위

- 이번 비교는 입력 전 기본 화면과 반응형 레이아웃을 대상으로 했다.
- 스캔 이후 카테고리·결과 화면의 데이터 상태는 이번 시각 비교 대상이 아니다.

final result: passed
