## CP2 Verification

- Command: `pnpm exec vitest run tests/http-server.test.ts tests/naver-blog-exporter.test.ts tests/ui/export-options-panel.test.tsx tests/ui/use-export-job.test.tsx tests/ui/app.test.tsx --silent`
- Result: passed (`5` files, `22` tests)

## Lifecycle And Secret Boundary Notes

- `tests/http-server.test.ts` verifies same-origin upload requests succeed only from `upload-ready` jobs and that serialized job payloads never contain `uploaderConfigJson` or the submitted JSON string.
- The same server test file also covers origin guard rejection and zero-candidate `download-and-upload` completion with `completed + skipped-no-candidates`.
- `tests/ui/use-export-job.test.tsx` keeps polling through `upload-ready` and `uploading` until `upload-completed`, confirming the same-job transition contract.
- `tests/ui/app.test.tsx` covers the visible upload form for `upload-ready`, successful submission through the results panel, terminal failure messaging, and the hidden form for zero-candidate completions.
- `tests/ui/export-options-panel.test.tsx` confirms the old Assets-tab uploader inputs are gone from the options surface.

## Manual Grep Check

- `rg -n "uploaderConfigJson|super-secret|token|secret|upload-ready|upload-completed|upload-failed|skipped-no-candidates|Origin|X-Requested-With" src tests .agents/plans/github-image-upload-phase/evidence`
- Result: only expected request-handling code, status coverage, and descriptive evidence references were found; no raw credential fixture or persisted secret value remained.
